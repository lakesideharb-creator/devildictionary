{
  "name": "the-devils-dictionary-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:standalone": "node scripts/build-standalone.mjs",
    "serve": "python3 -m http.server 4173",
    "test": "node --test tests/game.test.js && node tests/dom-e2e.mjs"
  },
  "devDependencies": {
    "happy-dom": "^20.0.10"
  }
}
